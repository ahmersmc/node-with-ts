// import { compareSync, hashSync } from 'bcrypt'

// const saltRounds = 10

// const encrypt = (password: string) => hashSync(password, saltRounds)
// const compare = (password: string) => compareSync(password, hash) // false

// export { encrypt, compare }
